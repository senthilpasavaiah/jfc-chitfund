import ComingSoonPage from './ComingSoonPage';
export default function DocumentsPage() {
  return (
    <ComingSoonPage
      title="Documents"
      note="Needs a file-storage decision (S3/Cloudinary) before member document uploads can work — see README.md."
    />
  );
}
